baseURL = 'https://fernglen.github.io/fernglen-2/'
languageCode = 'en-nz'
title = 'Fernglen Gardens'

cleanDestinationDir = true
# GitHub supports only "/"" and "/docs" as base directory for publishing a web site
publishDir = 'docs'

[menus]
    [[menus.main]]
        pageRef = '/'
        weight = 1
    [[menus.main]]
        pageRef = '/about'
        weight = 2
    [[menus.main]]
        pageRef = '/history'
        weight = 3
    [[menus.main]]
        pageRef = '/walks'
        weight = 4
    [[menus.main]]
        pageRef = '/fauna'
        weight = 5
    [[menus.main]]
        pageRef = '/events'
        weight = 6
    [[menus.main]]
        pageRef = '/visits'
        weight = 8

# Clear taxonomies to avoid creating stuff we don't need
[taxonomies]
