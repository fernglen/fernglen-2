baseURL = 'http://fernglengardens.nz/'
languageCode = 'en-nz'
title = 'Fernglen Gardens'

cleanDestinationDir = true
# GitHub supports only "/"" and "/docs" as base directory for publishing a web site
publishDir = 'docs'

[menus]
    [[menus.main]]
        pageRef = '/'
        weight = 1
    [[menus.main]]
        pageRef = '/about'
        weight = 2
    [[menus.main]]
        pageRef = '/history'
        weight = 3
    [[menus.main]]
        pageRef = '/walks'
        weight = 4

[taxonomies]
